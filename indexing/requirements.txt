# 전북소방 구조 AI — 인덱싱 파이프라인 의존성 (Python 3.11 권장)
pymupdf>=1.24            # PDF 텍스트 추출
python-pptx>=0.6.23      # PPTX 슬라이드/노트 추출
Pillow>=12.3.0           # python-pptx 이미지 처리 보안 하한
supabase>=2.7            # documents/chunks 적재 (service role)
python-dotenv==1.2.3     # ../.env.local 로드
idna==3.19               # HTTP 클라이언트 보안 하한 고정

# 임베딩 제공자: openai (기본)
openai>=1.40
# OpenAI SDK의 전이 HTTP 의존성: 압축 응답·SSE·업로드 헤더·SOCKS TLS
# 보안 수정 버전으로 고정한다. Python 3.10 이상(이 프로젝트는 3.11 권장).
httpx2==2.12.0
httpcore2==2.12.0

# 임베딩 제공자: bge (옵션) — BGE-M3 자체 호스팅
# sentence-transformers>=3.0

# serve.py (BGE-M3 임베딩 HTTP 서비스) 사용 시 (옵션)
# fastapi>=0.110
# uvicorn>=0.30
