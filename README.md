# 전북소방 구조 교육훈련 AI

소방 교육자료를 근거로 답변하고 훈련계획과 교안을 만드는 **구조대원 업무지원 플랫폼 PoC**입니다.
AI 튜터는 인덱싱된 자료를 검색해 출처와 페이지를 함께 제시하며, 근거가 부족하면
"관련 교육자료에서 확인되지 않습니다"라고 안내하도록 설계했습니다.

현재는 중간보고와 사용성 검증을 위한 PoC 단계이며, 검증 이후 애플리케이션, 데이터베이스,
LLM 및 임베딩 서비스를 내부망 서버로 이전하는 것을 목표로 합니다.

## 2026-09-06 AI 자료제작 출력·검토·편집 개선

훈련계획·교안·PPT의 출력 내용 보존과 제작 후 수정 흐름을 개선했습니다.

- **PPT 내용 보존·미리보기:** 타임라인·의사결정 흐름에서 뒤쪽 문장·단계가 빠지던 문제를
  수정했습니다. 미리보기와 PPTX가 같은 배치 정보를 사용하며, 선택 장 확대·이전/다음 장 이동,
  긴 문장 여러 줄 편집, 원문 그림 사전 확인을 제공합니다. 원문 그림은 확인한 페이지를 우선하고
  다운로드에서도 접근권한을 다시 확인합니다. 메모리의 임시 이미지는 저장본에 포함하지 않습니다.
  비교 구도로 바꿀 때도 기존 단계를 유지하며 앞 2개 기준과 나머지 추가 단계를 함께 표시합니다.
- **슬라이드 수:** 시간별 10~12/14~18/18~20장은 권장 범위로 안내하고, 편집한 본문은
  6~20장을 허용합니다. 화면·API·DB의 기준을 맞췄으며 안전·평가·SOP·원문 근거 검증은 유지합니다.
- **문서 정보·표 편집:** 현재 문서의 일자·장소·대상·시간을 Word·텍스트 복사에도 반영합니다.
  한국어 항목에 잘못 남은 줄바꿈 표기를 공통 정규화하며 코드·경로·URL은 보존합니다.
  기존 단계 시간표와 평가 항목을 표로 편집하고 화면·DOCX 표로 출력합니다. 저장은 기존 본문
  문자열과 호환되며 한글은 기존 표준 양식 안에 같은 본문을 채웁니다.
- **문단별 근거 확인:** 각 문단의 ‘근거 보기’에서 현재 활성 원본의 관련 구절과 정확한 페이지를
  확인합니다. 생성·저장과 같은 제한된 서버 reader를 이용하고 조회 실패·출처 불일치는 숨기지
  않습니다. 관련 구절은 원문 확인을 돕는 후보이며 문장별 사실성 판정을 뜻하지 않습니다.
- **다운로드 직전 검증:** 기술 수치가 없는 자료도 원본의 활성 상태·정확한 제목·페이지와
  현재 SOP 근거를 다시 확인합니다. 출처 변경이나 검색 장애를 확인 완료로 처리하지 않습니다.
- **생성 전 구성 확인:** 새 영속 작업은 기본적으로 목차·중점·시간·부족 근거를 확인한 뒤 본문을
  생성합니다. ‘전체 생성 전에 구성 확인’을 해제하면 기본 구성으로 바로 진행합니다. 대기 중에는
  모델 실행을 유지하지 않으며 나중에 같은 작업 주소로 돌아올 수 있습니다. 수정한 행동 요구와
  기술 조건을 후속 검색·본문 생성에서도 유지합니다. 부족 근거 추가 검색은 기존 최대 2회 안에서
  사용자가 입력한 조건과 안전·중단 항목을 우선합니다.
- **작업 중단·문제별 보완:** 명시적 중단 시 실행 토큰을 폐기하고 저장된 단계를 보존합니다.
  필요하면 중단한 작업을 재개할 수 있습니다. 미통과 작업은 문제 위치·이유·권장 조치를 보여
  주고 선택한 항목을 보완합니다. 미검증 초안은 별도 화면에서 편집한 뒤 서버에 보관하여 전체
  검사를 다시 수행하며, 완료 결과의 저장·공유·내보내기와 분리합니다.
- **부분 재생성 비교·되돌리기:** 다른 문단의 내용과 기존 배정 시간을 고려해 수정안을 만들고,
  전체 문서를 다시 검사한 뒤 변경 전후를 비교해 적용합니다. 적용 전 원본을 유지하며, 적용 후
  다른 편집을 하기 전에는 한 단계 되돌릴 수 있습니다. 다른 탭의 개정과 충돌하면 사용자 입력을
  자동으로 최신본으로 교체하지 않고 충돌을 알립니다.

기존 설치는 앱 배포 전에 `20260906010516_generation_job_review_controls.sql`과
`20260906010707_align_edited_slide_count.sql`을 순서대로 적용해야 합니다.
전자는 검토용 공개 필드와 작업 상태만 추가하고, 후자는 내부 품질 함수의 슬라이드 장수 기준만
맞춥니다. 기존 생성물·공유 상태·출처·RLS·개정 번호 보호는 유지합니다.
주메뉴의 최신 수정 내역은 **`자료제작 출력·구성 검토·편집 개선`**으로 표시합니다.

검증: 자동 테스트 1,053개 통과(외부 연결 선택 검사 32개 제외), TypeScript·ESLint·프로덕션 빌드 통과.
실제 브라우저에서 부분 재생성 비교·적용·되돌리기, 시간 합계 검증, 초안 닫기 후 입력 보존,
모바일 초안 편집·슬라이드 확대를 확인했습니다. 작업 API를 모의한 브라우저 검증에서 승인·중단,
다른 화면의 개정 충돌 후 입력 보존과 명시적 최신본 교체도 확인했습니다.
브라우저에서 받은 Word 파일은 문서 정보와
시간표 6개를 보존했습니다. PPT는 7개 주요 구도의 본문 14장을 실제 PDF로 렌더해 권장 길이
경계에서 내용 누락·겹침을 검사했습니다. Windows PowerPoint의 실제 글꼴 환경은 별도 확인이
필요합니다. 운영 DB 변경 전후 기존 생성물 10건의 전체 내용 해시가 같고 RLS·내부 필드
비공개·일반 사용자 직접 변경 차단도 유지됨을 확인했습니다.

## 2026-09-06 자료제작 목록 하단 배치·접기

AI 자료제작(`/generate`)의 ‘이어서 작업하기’를 제작 입력·결과 아래로 옮겼습니다.
‘이어서 작업하기’와 ‘저장한 자료’는 각각 기본 접힘 상태이며, 제목 줄에서 개수를 확인하고
필요한 목록만 펼치거나 접을 수 있습니다. 두 목록은 동시에 펼칠 수 있고, 목록을 접어도
현재 열어 본 저장 자료의 상태는 유지됩니다. ‘전체 보기’는 저장한 자료를 펼친 안쪽에 있습니다.
브라우저 기본 펼침 요소를 사용해 키보드 Enter·Space로도 조작할 수 있습니다.
별도의 저장한 자료·공유 자료 페이지와 기존 초안·작업 재진입, 저장·삭제 흐름은 유지합니다.
주메뉴 수정 내역은 **`자료제작 목록 하단 배치·접기`**로 표시합니다.
검증: 기존 초안·접근성 테스트 41개, TypeScript·ESLint·프로덕션 빌드 통과.

## 2026-09-05 조건별 검색·자료 근거 보완

앞선 검색 정렬·본문 검토 개선에 이어 **조건별 부족 근거 검색 → 한국어 표현 보완 → 자료제작
목차별 근거 확인**을 적용했습니다. 이 배포 당시 주메뉴 최종 수정 일시 아래의 한 줄은
**`조건별 검색·자료 근거 보완`**입니다. 수정 일시는 배포 빌드 시각을 한국시간으로 표시합니다.

- **AI 튜터·공통 주제 검색:** 기존 검색 규칙이 질문에서 명시 조건을 두 개 이상 찾으면
  각 조건의 첫 표현으로 검색하고, 확보한 원문 후보를 재사용합니다. 주제·상황 단서가 부족한
  조건에만 다른 표현을 한 번 더 배정합니다. 보완은 분야 범위당 최대 3개 키워드 질의이며,
  전체 12개·동시 4개 상한 안에서 실행합니다. 튜터의 기존 분야 0건 재검색은 별도 범위입니다.
  단순 질문에 조건 분해용 모델 호출을 추가하지 않습니다. 임의의 모든 복합 질문을 이해하는
  의미 판정기는 아니며, 인명 상황·장비 절차·평가 등 현재 정의한 검색 조건에 적용됩니다.
- **한국어 표현:** 실제 실패한 `몸이 꿰뚫려`, `박혔`, 조건이 함께 제시된 `발이 땅에 닿지 않음`,
  화학보호복 `입기·벗기`, `SCBA` 등을 기존 매뉴얼 용어와 연결합니다. 원문을 바꾸지 않고
  최대 8개 검색어만 추가합니다. 부정문·벌집·드론·일반 물체는 인명 상황으로 확대하지 않고,
  산소통을 공기호흡기로 간주하지 않습니다. 기존 임베딩·색인·자료는 그대로 사용합니다.
- **답변의 근거 범위:** 최종 참고 자료에서 단서가 부족한 조건을 답변 모델에 전달합니다.
  복합 상황은 먼저 통합 절차의 확인 여부를 밝히고, 개별 근거·적용 차이·미확인 범위를
  구분합니다. 검색 단어나 인용의 일치는 적용 가능성·사실 정확성의 보증이 아닙니다.
  개별 자료를 이어 붙인 실행 절차를 만들지 않도록 하고, 부족 범위를 코퍼스 전체의 부재로
  단정하지 않습니다. 일부 근거가 있는 답변을 별도 `partial` 상태로 평가합니다.
  재순위 모델이 모든 후보가 무관하다고 명시하면서 빈 목록을 선택하면 무관한 자료로 다시
  채우지 않습니다. 모델 장애·모순된 응답은 기존 후보로 복귀하고 보호된 개별 근거는 보존합니다.
  최종 참고 자료가 비면 서버가 답변 모델을 추가 호출하지 않고 표준 안내만 반환·저장합니다.
- **AI 자료제작:** 실제 `/api/generate/jobs` Workflow에서 목차의 기술 요구사항마다 출처와
  연속 원문 인용을 받아 대조합니다. 부족 항목은 작업 전체에서 최대 2개만 추가 RAG 검색하며
  선택 분야 밖이나 분야 전체 표본으로 넓히지 않습니다. 새 원문이 생길 때만 연결 검토 모델을
  최대 1회 호출합니다. 완료한 조회·검토는 체크포인트에 저장해 재개 시 반복하지 않으며,
  초안이 시작된 과거 작업의 근거를 뒤늦게 바꾸지 않습니다. 미해결 조건은 초안·선택 보완·
  최종 경고에 유지하고 기존 최종 grounding 검토, SOP 확인, 저장 권한·개정 검증도 유지합니다.
  추가 RAG 1회에는 확장·임베딩·키워드·벡터 조회가 포함되며 장애 재시도는 별도입니다.
  추가 조회가 없어도 목차의 인용 출력 필드 때문에 모델 토큰은 늘 수 있습니다.
- **자료 저장 기준 일치:** 운영에서 완성된 훈련계획을 저장하며, 문서는 출처를 말미에 모으는데
  DB가 본문 인라인 출처를 요구해 저장을 거부하는 불일치를 발견했습니다. 후속 마이그레이션
  `20260905140458_align_generated_document_endnote_evidence.sql`로 문서의 핵심 품질·SOP 검사를
  검증된 말미 출처 목록과 연결했습니다. 실제 원본·활성 여부·분야·SOP 번호와 명칭·시간·안전·평가,
  알 수 없는 본문 출처 거절과 슬라이드의 같은 장 출처 검사는 유지합니다. 데이터와 RLS·개정
  보호는 바꾸지 않습니다. 기존 설치는 해당 마이그레이션을 적용해야 합니다.

**실제 자료 전후 비교:** `7429c76` 기준 코드와 변경 코드를 같은 운영 자료에서 읽기 전용으로
7문항씩 실행했습니다. 아래는 검색 근거 회수·호출량 점검이며 전체 답변 정확도는 아닙니다.

| 비교 항목 | 변경 전 | 변경 후 |
| --- | --- | --- |
| 새 표현 질문에서 구급 269~270쪽·로프 706쪽 회수 | 0/2 | 2/2 |
| 기존 복합 질문·말바꾸기의 지정 출처 회수 | 4/4 | 4/4 |
| 화학보호복·드론·인명구조사 근거 키워드 | 12/12 | 12/12 |
| 기존 복합 질문 2개의 키워드 조회 | 각각 5회 | 각각 3회 |
| 인명구조사 질문의 키워드 조회 | 7회 | 5회 |
| 7문항 검색 시간 평균 | 7.57초 | 6.10초 |
| 무근거 질문의 최종 참고 자료 | 무관 후보 8개 | 0개 |

새 표현 질문은 `나뭇가지에 몸이 꿰뚫려 땅에 발이 닿지 않는 구조대상자`입니다.
새 표현의 최종 검색은 7.44→5.73초였으며, 초기 보완 실행에서는 10.18초도 관측했습니다.
변경 전에는 근거가 없어 재순위가 생략됐지만 변경 후에는
찾은 자료의 재순위를 수행했습니다. 각 문항 한 번의 소표본으로 네트워크와 모델 변동이 있으므로
평균 시간을 보편적인 속도 향상으로 해석하지 않습니다. 기존 드론 분야 재검색의 `degraded=true`는
남아 있으며 이번 개선 성과로 계산하지 않습니다. 자료 없음 질문의 검색 후보 수와 답변의 올바른
거절 여부도 별도로 평가합니다. 동기 호환 API의 검사를 실제 Workflow 검증으로 대체하지 않습니다.

검증: 후속 DB 저장 수정까지 전체 테스트 **987개 통과·외부 연동 등 32개 생략**,
TypeScript·ESLint 통과. 검색·자료제작 변경의 프로덕션 빌드도 통과했습니다.
실제 자료와 답변 모델을 사용하는 `eval/questions.partial.jsonl`의 4문항은 최종 자동 기준을
모두 충족했습니다(부분 답변 3개·표준 무근거 응답 1개, 기준 100%). 초기 점검에서 드러난
미확인 범위 명시 누락과 무근거 안내 뒤의 추측성 설명을 보완한 뒤 다시 확인한 결과입니다.
자동 기준 통과는 각 문장의 정확성이나 현장 적용 가능성 보증이 아닙니다.

운영 브라우저에서도 새 표현의 부분 답변·출처 8개와 무근거 질문의 표준 안내만 저장되는 것을
확인했습니다. 실제 훈련계획 작업은 목차 요구사항 10개를 원문 인용과 연결했고, 모두 확보되어
추가 검색은 하지 않았습니다. 진행 중 새로고침 후 같은 작업을 복구해 5개 섹션 생성을 완료했습니다.
DB 기준 정렬 적용 뒤, 저장에 실패했던 동일 결과를 화면에서 다시 저장해 출처 23개와 함께
비공개 저장됐고 DB 핵심 품질·SOP 검사도 통과했습니다. 새 PGlite 검사는 계획·교안의 말미 출처
저장과 재적용·revision을 확인하고, 출처·SOP·시간·안전·평가를 훼손한 11개 사례의 직접 쓰기를
거절합니다. 이 생성 사례는 이미 충분한 근거가 있는 경로의 확인이며, 추가 검색 경로는 회귀검사로
검증했습니다.

## 2026-09-05 검색 정렬·본문 검토 개선

앞선 1·2단계 작업은 **관련성 정렬 후 후보 제한**과 **재순위 판단의 본문 범위 확대**에
한정했습니다. 당시 보류한 후속 개선의 적용 범위와 검증은 위의 최신 내역을 참고하세요.
이 단계에서는 기존 임베딩·원본·분야 범위·보호 근거와 답변 생성 규칙을 유지했습니다.

- 키워드 검색은 `search_rag_rescue_keywords` RPC에서 활성·분야 조건을 적용하고,
  `ts_rank_cd DESC, id ASC`로 정렬한 **다음** 후보 수를 제한합니다. 제한된 행을 가져온 뒤에만
  관련성을 계산해서 좋은 자료를 놓칠 수 있던 순서를 고쳤습니다. 기존 본문 GIN 인덱스와 호출자
  RLS를 사용하며 인증 사용자·서비스 역할만 실행할 수 있습니다. PostgreSQL FTS 순위이며 BM25는
  아닙니다. 튜터와 자료제작이 공유하는 주제 검색 후보 조회에 적용됩니다.
- 튜터의 재순위 판단은 앞 350자 대신 2,000자 이하 청크의 **본문 전체**를 읽습니다.
  확인한 운영 활성 자료는 최대 1,201자여서 모두 전체 전달 대상입니다. 이보다 큰 과거 자료는
  질문 관련 연속 2,000자만 사용하고 앞뒤 생략 여부를 표시합니다. 후보 20개·질문 8,000자·전체
  프롬프트 60,000자 상한을 두고, 모델·재순위 호출 횟수·45초 제한·실패 시 기존 순서 복귀는
  유지합니다. 자료제작의 별도 최종 선택 방식과 SOP 전용 검색은 이번에 바꾸지 않았습니다.

**운영 전후 비교:** 기존 `cdb0d7c`(A), 정렬만 수정(B), 정렬+본문 확대(C)를 고정된 코드로
같은 운영 자료에서 읽기 전용 비교했습니다. 원문 복합 질문·말바꾸기·화학보호복·드론·인명구조사·
자료 없음 경계 질문 6개를 각각 한 번 실행했습니다. 답변 생성·HTTP 인증·대화 저장 시간은
제외하고 질의 확장·임베딩·검색·재순위를 측정했습니다.

| 비교 항목 | 기존 A | 정렬 B | 정렬+본문 C |
| --- | --- | --- | --- |
| 복합 질문 2개에서 구급 270쪽·로프 706쪽 동시 회수 | 2/2 | 2/2 | 2/2 |
| 일반 질문 3개의 근거 키워드 점검 | 각 4/4 | 각 4/4 | 각 4/4 |
| 6개 검색 시간 평균 | 5.93초 | 6.13초 | 7.39초 |
| 기존 드론 분야 재검색 외 새 `degraded` | 없음 | 없음 | 없음 |

이 결과는 기존 근거의 회귀 점검이며 전체 정답률 향상 증거가 아닙니다. 질의 확장 모델의 문구·
검색 계획과 네트워크 지연이 달라질 수 있어 전체 시간 차이를 한 변경의 효과로 해석하지 않습니다.
정렬 자체도 DB 작업을 추가합니다. 대표 광역 쿼리의 내부 SELECT는 기존 활성 GIN을 사용했고
단독 실행 약 0.63초였으며, 실제 왕복 시간은 더 길었습니다. 검색 요청 상한 12회·동시 4회는
유지하지만 요청마다 생성된 검색어가 달라 실제 호출 수는 변할 수 있습니다.

**같은 후보로 본문 범위만 비교:** 3개 질문의 후보·선택 수를 고정해 재순위 모델을 각각 한 번씩
호출했습니다. 입력 토큰은 기존 대비 1.24~2.09배였고, 재순위 시간은 원문 1.95→2.55초,
화학보호복 4.09→4.09초, 인명구조사 1.26→1.17초였습니다. 실제 화학보호복 자료의 368번째
문자에 있는 착용·실격 조건, 다른 자료의 572번째 문자 점검 조건, 평가자료의 536번째 문자
감점·실격 조건이 기존 350자 입력에는 없고 변경 후에는 전달됨을 확인했습니다. 입력 정보 누락을
줄인 확인이며, 모델이 모든 조건을 올바르게 해석한다는 보증은 아닙니다. 모델 호출을 추가하지
않아도 토큰 비용은 늘 수 있고 속도 개선으로 표현하지 않습니다.

최종 C로 원문 2개·말바꾸기·화학보호복·인명구조사 **5개를 동시에 검색**했을 때
7.07~9.30초(평균 8.06초), 시간 초과 0건·`degraded=false`였고 기존 핵심 근거를 유지했습니다.
이는 해당 동시 요청 묶음의 확인이며 운영 처리량 보증은 아닙니다. 자료 없음 질문은 세 방식 모두
검색 단계에서 관련성 낮은 후보를 반환할 수 있어, 검색 결과 수만으로 올바른 답변·거절 여부를
판정하지 않았습니다. 기존 드론의 분야 재검색 안내도 이번에 해결한 문제로 계산하지 않습니다.

운영 DB에는 `20260905124809_rank_rag_keyword_candidates.sql`을 먼저 적용했습니다.
활성 자료 20,938개 보존, 호출 권한·RLS·실제 원문 검색을 확인했습니다. 앱 배포 전 이 마이그레이션이
필요하며, 다른 `RAG_TABLE`을 사용하는 설치는 같은 인자의 `search_<테이블명>_keywords` 함수가
필요합니다. 주메뉴 최종 수정 일시 아래의 한 줄은 **`검색 정렬·본문 검토 개선`**으로 갱신했습니다.

검증: 전체 테스트 892개 통과·외부 연동 평가 등 32개 생략, TypeScript·ESLint·프로덕션 빌드 통과.
신규 PostgreSQL 회귀검사는 제한 범위 밖 최고 관련 후보의 회수·역할별 권한·RLS·입력 경계를,
재순위 회귀검사는 350자 이후 조건 전달·자료 번호 대응·입력 상한·모델 실패 복귀를 확인합니다.

## 2026-09-05 복합 질문 검색 및 수정 내역 표시

긴 상황 질문에서 문장 앞부분이 검색어를 모두 차지하던 문제를 보완했습니다. 원문 핵심어와
확장어에 검색어 자리를 나누어 배정하고, 관통상·매달린 요구조자처럼 명시된 조건은 개별 근거를
찾아 최종 참고 자료에 유지합니다. 개별 근거는 해당 질문 전체의 행동절차가 검증됐다는 뜻이 아닙니다.
여러 조건의 자료를 합칠 때는 적용 대상·전제·예외와 추가 확인이 필요한 범위를 구분합니다.
부분 답변에 미확인 안내 문구가 들어 있어도 근거 자료가 통째로 사라지지 않도록 수정했습니다.

이 배포 당시 주메뉴 하단의 `최종 수정 · YYYY.MM.DD HH:mm` 바로 아래에는
**`복합 질문 검색·근거 안내 개선`**을 같은 작은 글씨로 한 줄 표시합니다.
최신 문구는 [`lib/web-release.ts`](lib/web-release.ts)의 `WEB_RELEASE_SUMMARY`에서 관리합니다.
긴 문구는 메뉴 폭 안에서 말줄임 처리하고 전체 문구를 툴팁과 보조기기에 제공합니다.
빌드 시각의 한국시간 표시와 기존 접속 집계는 유지합니다.

검색 확장·재순위 모델 호출을 추가하지 않고 기존 검색 계획을 재사용합니다. 키워드 검색은
검색 계획당 최대 12회·동시 4회 제한을 유지합니다. 분야 검색이 0건일 때의 기존 재검색은 별도입니다.
검색 품질, 실제 호출량, 응답 시간은 별도로
비교하며, 일부 질문의 근거 회수 개선을 전체 정답률 또는 응답 속도 개선으로 표현하지 않습니다.

**검색 효율 비교:** 운영 코퍼스를 읽기 전용으로 조회하고 이전 `7b78a01` 코드와 변경 코드를
같은 원문 질문으로 번갈아 3회씩 실행했습니다. 질의 확장·임베딩·재순위를 포함한 실제 검색이며,
답변 생성·HTTP 인증·대화 저장 시간은 제외했습니다.

| 원문 질문 반복 비교 | 이전 | 변경 후 |
| --- | --- | --- |
| 관통상 처치와 나무의 요구조자 구조 근거 동시 회수 | 0/3회 | 3/3회 |
| 관통상 자료 | 3회 모두 누락 | `소방전술3(구급)` PDF 270쪽을 3회 모두 확보 |
| 나무의 요구조자 구조 자료 | 일반 구조·로프 자료에 치우침 | `THE 로프 구조 2025` PDF 706쪽을 3회 모두 확보 |
| 검색 시간 평균 / 중앙값 | 7.35초 / 7.17초 | 4.74초 / 4.61초 |
| 검색 시간 범위 | 3.99~10.90초 | 3.99~5.63초 |
| 검색 보조 모델 호출 | 2회 | 2회 |
| 임베딩 / 벡터 검색 | 각 1회 | 각 1회 |
| 키워드 DB 검색 | 3회 | 5회 |

이번 사례에서는 같은 모델 호출 횟수로 필요한 개별 근거를 더 잘 찾았습니다. DB 조회는 2회 늘었습니다.
최종 반복에서는 평균 시간이 짧았으나, 보완 중 별도 반복에서는 이전 6.59초·변경 후 6.83초였을 만큼
편차가 있어 전반적인 속도 향상으로 해석하지 않습니다. 최종 6회 모두 검색 장애 표시는 없었습니다.
소규모 반복 결과이며 캐시·모델·네트워크 변동을 포함합니다.

추가로 5개 검색을 동시에 실행했을 때 모두 두 핵심 자료를 확보하고 `degraded=false`였으며,
검색 시간은 4.68~7.06초였습니다. 이는 해당 요청 묶음의 결과이며 운영 동시 사용자 수 보증은 아닙니다.
표현·순서를 바꾼 복합 질문도 두 자료를 회수했습니다. 기존 화학보호복·드론·인명구조사 질문은
각 4개 근거 키워드 점검 항목을 유지했습니다. 드론은 기존에도 분야를 지정한 1차 검색이 0건이어서
전 분야 재검색과 `degraded=true` 안내가 발생했으며, 이번 변경으로 해결된 항목으로 계산하지 않습니다.
이 검색 결과만으로 해당 분야의 임베딩 자료 자체가 없다고 단정하지 않습니다.

실제 기본 모델 답변에서는 복합 질문을 `확인된 범위 / 적용 차이 / 추가 확인이 필요한 범위`로
나누고, 결합된 상황의 전용 절차가 확인되지 않았음을 표시하는지 점검했습니다. 없는 장비를 전제로 한
질문은 표준 미확인 답변을 유지했습니다. 이 점검은 해당 답변의 구조·근거 경계 확인이며, 의학적·현장
적합성 인증이나 모든 질문의 환각 방지 보증은 아닙니다.

검증: 전체 테스트 875개 통과·외부 연동 평가 등 32개 생략, TypeScript·ESLint·프로덕션 빌드 통과.
실제 사이드바 컴포넌트를 240px 메뉴와 짧은 화면에서 렌더해 수정 내역 한 줄 및 가로 넘침 없음을
확인했습니다. DB 스키마와 임베딩은 변경하지 않았습니다.

운영 반영: 기능 커밋 `06f0cb7`을 GitHub·GitLab `main`에 푸시하고 Vercel `READY`를 확인했습니다.
로그인한 운영 화면에서 수정 내역 한 줄, 원문 질문의 조건별 답변과 구급 PDF 270쪽·로프 구조 PDF
706쪽을 포함한 근거 자료 8개를 확인했습니다.

## 2026-09-05 코드리뷰 후 개선

AI 튜터의 검색 누락, AI 자료제작의 편집 복구와 근거 검증, 모바일 가독성을 보완했습니다.
아래 내용은 이번 변경 사항과 개발·운영 검증 기록입니다.

**운영 반영(2026-09-05):** 기능 변경 커밋 `49989f8`을 GitHub·GitLab의 `main`에 반영하고,
Vercel 운영 배포 `READY`를 확인했습니다. 배포 전에 메시지 복구·개인 초안 마이그레이션 2개를
적용했으며 기존 메시지·공식 저장 자료·뉴스가 유지되고 초안 소유자 RLS가 활성화된 것을 확인했습니다.
로그인한 운영 홈 화면에서 누적 수치 아래 수정 시각, 홈·동향 목록의 최근 30일 기준을 확인했습니다.
운영 뉴스 수집 API는 HTTP 200·8건 추가·피드 실패 0·요약 누락 0으로 복구됐습니다.
목록 검수에서 발견한 구조 동향과 무관한 골프 행사 1건은 숨김 처리하고 이후 수집에서도 제외합니다.

| 개선 영역 | 변경된 동작 |
| --- | --- |
| 자연스러운 질문 | `신규대원인데 화학보호복 착용 절차를 알려줘`의 자기소개를 검색 주제로 오인하지 않습니다. 주제 일치 필터는 유지합니다. |
| 이어지는 질문 | `주의사항은?`, `다시 쉽게 설명해줘`에는 앞선 주제를 이어 붙이고, 새 장비·주제를 명시하면 이전 주제와 분리합니다. 등급 변경도 다음 질문에 유지합니다. |
| 출처 확인 | 검색 컨텍스트에 제공한 문서·페이지를 모두 반환합니다. 출처를 3개로 잘라 뒤쪽 근거 페이지를 숨기지 않습니다. 출처 목록은 검색 참고 자료이며 주장별 사실 검증 완료 표시가 아닙니다. |
| 튜터 오류 복구 | 오류 원인, 동일 질문 재시도, 질문 복원, 저장된 대화 확인을 제공합니다. 요청 식별자로 질문 저장 중복을 막고, 검색 장애와 답변 저장 실패를 표시합니다. 검색 장애 경고는 대화를 다시 열어도 유지합니다. |
| 읽기 쉬운 답변 | Markdown 제목·강조·목록·표를 실제 구조로 표시합니다. 넓은 표는 내부에서 스크롤하며, 모델이 만든 HTML과 외부 이미지는 실행·자동 요청하지 않습니다. |
| 자료제작 재진입 | `/generate`와 저장한 자료 화면에서 최근 생성 작업과 개인 편집 초안을 다시 엽니다. 완료 후 아직 공식 저장하지 않은 결과도 작업 목록에서 찾을 수 있습니다. |
| 개인 초안 자동보관 | 편집 중인 빈 항목도 서버에 보관합니다. 본인만 읽고 쓸 수 있으며, 개정 번호가 다른 편집을 덮어쓰지 않습니다. 충돌하면 별도 초안으로 보관하고, 불필요한 초안은 최근 작업 목록에서 삭제할 수 있습니다. 공식 저장본과 생성 작업은 유지합니다. |
| 편집·디자인 | 현재 문서의 날짜·장소를 직접 수정합니다. `편집 닫기`와 공식 `저장`을 구분하고, 문서 본문 16px·주요 동작 버튼 대비 4.5:1 이상·원문 페이지 링크를 적용했습니다. |
| 한글 대체 파일 | 한글 작성 서버 미설정·장애 시 브라우저에서 만드는 HWPX에도 훈련 일시·장소·대상·교육시간을 별도 항목으로 보존합니다. |
| 웹 수정 시각 | 주메뉴 하단 접속 현황의 누적 수치 아래에 `최종 수정 · YYYY.MM.DD HH:mm`을 작은 글씨로 표시합니다. 한국시간(KST)으로 표시하며, 해당 웹 버전의 빌드 시각을 고정해 접속·새로고침 때 바뀌지 않습니다. |
| 최신 구조 동향 | 홈·동향 목록과 자동수집에 한국시간 오늘 포함 최근 30일 기준을 적용합니다. 오래된 고정글·미래 기사·발행일 미상 자동수집 기사는 제외하며, 과거 데이터는 관리자 목록에 보존합니다. |
| 뉴스 수집 복구 | 기존 URL 부분 유니크 인덱스와 맞지 않던 `upsert`를 수정했습니다. 중복 기사만 건너뛰고 신규 기사를 저장하며, RSS·AI 응답 시간을 제한하고 일부 수집·요약 실패를 관리자에게 경고합니다. |
| 생성 근거 검토 | 정밀 생성 Workflow에서 구조 검사에 더해 기술 수치·원문 적용 조건·사용자 요청 조건을 검토합니다. 수정한 부분은 다시 검토하며, 미해결 핵심 오류나 검토 실패를 완료로 표시하지 않습니다. |
| 편집 후 수치 검토 | 앱의 저장·공유 API와 파일 다운로드·텍스트 복사 직전 검증에서 서버가 실제 출처 본문을 다시 읽습니다. 브라우저가 보낸 출처 미리보기만으로 수치를 승인하지 않습니다. |
| 평가 도구 | 질문 반복·거절문·검색 장애만으로 높은 점수를 받던 평가를 보완했습니다. 결과를 사실 정확도 대신 `자동 기준 충족률(점검률)`로 표시하고, 실제 비동기 작업 생성→상태 조회 평가 모드를 추가했습니다. |

**동일 조건으로 확인한 개선:** 운영 코퍼스를 읽기 전용으로 조회한 결과, 위의 `신규대원인데…`
질문은 수정 전 검색 0개·답변 거절에서 수정 후 검색 8개·상세 착용 절차 답변으로 바뀌었습니다.
질의 확장·재순위가 켜진 기본 설정에서도 확인했습니다. `주의사항은?`도 운동·디브리핑 자료 대신
화학보호복 자료로 연결되고, 기존에 누락된 p.260 출처가 목록에 포함됩니다. 기존 대표 4문항은
각각 8개를 검색하고 기존 근거 점검 항목 4/4·4/4·5/5·6/6을 유지했으며 모두 `degraded=false`였습니다.
이 비교는 대표 질문의 개선 증거이며 전체 질문의 정답률을 뜻하지 않습니다.

**구조 동향 중단 확인:** 운영 DB의 마지막 수집은 2026-07-08 06:20 KST이고, 최신 기사 날짜는
7월 5일이었습니다. 7월 8일 배포에서 다시 도입된 `ON CONFLICT(url)`과 운영의 부분 유니크
인덱스가 맞지 않아 `42P10`이 발생하는 것을 쓰기 없는 `EXPLAIN`으로 재현했습니다. Cron과
운영 인증 키 설정은 유지되어 있었습니다. 과거 Cron 실행 로그는 보관 기간이 지나 확인하지 못했습니다.
새 코드는 기존 인덱스를 유지한 채 행별 저장과 URL 중복 처리로 해결하므로 뉴스용 추가 DB 변경은 없습니다.

뉴스의 최근 30일은 양 끝 날짜를 포함합니다(9월 5일 기준 8월 7일~9월 5일). 화면은 등록일이
아닌 기사 발행일을 기준으로 하며, 발행일이 없는 수동 등록글만 한국시간 등록일로 판단합니다.
자동수집은 날짜가 없는 기사에 오늘 날짜를 붙이지 않습니다. RSS 전체 응답을 기간·최신순으로
정리한 뒤 후보를 선택하고, 국내·해외 피드를 함께 조회해 실행당 최대 8건을 추가합니다.
정기 수집은 기존 매일 06시 KST 일정(Vercel Hobby는 해당 시간대 내 실행)을 유지합니다.
배포 후 관리자 **동향 관리 → 지금 수집**으로 즉시 갱신할 수 있습니다. 수정 코드의 실제 RSS
조회에서는 최근 기간 후보 90건·피드 실패 0건을 확인했고, 운영 DB의 새 기간 조회도 오류 없이
실행됐습니다. 이 초기 검증은 읽기 전용이었으며, 이후 운영 수집·배포 확인은 상단 반영 기록과 같습니다.

실제 정밀 모델로 진행한 소규모 근거 검토에서는 정상 원문 요약을 통과시키고, 같은 숫자를 다른
장비에 적용한 문장과 `실내만 사용` 조건을 어긴 문장을 각각 차단했습니다. 정량 대조는 단위가 있는
수치의 존재 여부를, 모델 검토는 적용 조건·행동·요청 제약을 점검합니다. **모델 검토 역시 오류가
가능하며, 편집 후 수치 검사만으로 모든 문장의 사실성이 보증되지는 않습니다.** 명확한 교육 평가율과
가상 실습 조건은 기술 한계값과 구분하고, 압력·농도·온도 같은 한계값은 사용자 입력만으로 승인하지 않습니다.

자동보관은 공식 저장·공유와 별도입니다. `자동보관됨`은 서버에서 보관 응답을 받았다는 뜻이며,
그때까지의 내용을 개인 초안에서 다시 열 수 있습니다.
앱 내 링크 이동과 새 생성은 미보관 내용을 먼저 보관하며, 새로고침·창 닫기는 브라우저 이탈 경고를
사용합니다. **네트워크/DB 장애와 SPA 뒤로 가기가 겹치거나 브라우저가 강제 종료되면 아직 확인되지
않은 편집이 유실될 수 있습니다.** 오류가 보이면 현재 화면을 유지하고 보관을 다시 시도해야 합니다.
데모 모드는 실제 개인 초안을 쓰지 않으며 실제 RAG·기술 검증을 수행하지 않았다고 표시합니다.

**기존 운영 환경 적용 순서:** 아래 두 마이그레이션을 순서대로 적용한 뒤 앱을 배포합니다.
메시지 복구 컬럼이 없으면 튜터가 저장 상태를 확인할 수 없어 오류를 표시합니다. 신규 설치용
`setup_new_project.sql`에도 두 변경을 포함합니다.

1. [`20260904222054_improve_tutor_recovery.sql`](supabase/migrations/20260904222054_improve_tutor_recovery.sql): 메시지 검색 장애 상태와 요청 식별자·중복 방지.
2. [`20260904222055_private_generation_drafts.sql`](supabase/migrations/20260904222055_private_generation_drafts.sql): 개인 초안, 소유자 RLS, 개정 번호·크기 제한.

DB 검증은 PGlite에서 실제 SQL과 RLS·CAS를 실행하고, UI·API·생성 Workflow 회귀검사는 Vitest로
확인합니다. 실제 Supabase 세션의 브라우저 자동보관과 Vercel Workflow 전체 실행은 마이그레이션·배포 후
별도로 확인해야 합니다. 실행 방법과 외부 API 평가 범위는 [`eval/README.md`](eval/README.md)를 참고하세요.

이번 변경의 최종 검증 결과:

- `npm test`: **853개 통과**, 명시 실행이 필요한 외부 연동 평가 등 **32개 건너뜀**.
- TypeScript·ESLint·데모를 끈 Next.js 프로덕션 빌드 통과.
- 데스크톱·390px 모바일 데모 화면에서 답변 목록 렌더링, 가로 넘침 없음, 통신 오류 안내,
  질문 복원·동일 질문 재시도 후 말풍선 중복 없음 확인.
- 날짜·장소 편집 후 실제 다운로드한 대체 HWPX의 ZIP/XML에서 날짜·장소·기존 본문 보존 확인.
- 운영 코퍼스의 읽기 전용 검색·실제 답변 비교와 정밀 모델 근거 검토는 위의 대표 사례 범위로
  수행했습니다. 데모 화면 검증을 실제 RAG 또는 운영 DB 저장 성공으로 간주하지 않습니다.
- 뉴스 날짜 경계·과거 고정글 제외·RSS 실패·중복 경쟁·요약 취소와 실제 PostgreSQL 부분 인덱스
  회귀를 확인했습니다. 뉴스 조회 화면에는 최근 30일 기준과 빈 결과를 명시합니다.

## 핵심 원칙

- **근거 우선**: 교육자료 검색 결과를 바탕으로 답변과 생성물을 작성합니다.
- **출처 추적**: 문서명과 페이지를 표시하고 자료실 원문으로 연결합니다.
- **환각 억제**: 근거가 없거나 불확실한 내용은 추측하지 않습니다.
- **현장 사용성**: 모바일 우선 UI, 큰 터치 영역, 명확한 상태 표시를 적용합니다.
- **권한 분리**: 사용자 데이터에는 RLS를 적용하고 관리자 기능은 서버에서 권한을 재검증합니다.

## 주요 기능

| 영역 | 내용 | 현재 상태 |
| --- | --- | --- |
| AI 튜터 | 스트리밍 질의응답, 분야 필터, 대화 이력, 출처·페이지, 답변 피드백 | PoC 구현 |
| AI 자료제작 | 주제 중심 근거검색, 유형별 고정 구성, 자동 품질 보완, 직접 편집·부분 재생성·저장·공유 | PoC 구현 |
| 문서 출력 | 한글 HWPX, Word DOCX, 발표자 노트가 포함된 PPTX 다운로드 | PoC 구현 |
| 자료실 | 원본 자료 목록, PDF 열람, AI 답변 출처에서 해당 페이지로 이동 | PoC 구현 |
| RAG 인덱싱 | PDF·HWPX·HWP 등 파싱, OCR, 청크 미리보기, 버전 교체, 원본 등록 | PoC 구현 |
| 구조 동향·공지 | 구조 사례 및 기술 동향 제공, 공지 열람과 관리자 작성 | PoC 구현 |
| 관리자 | 이용 통계, 인기 질문, 자료·사용자·공지·동향 관리 | PoC 구현 |

기본 분야 분류는 **산악·수난·화재·구급·일반구조**이며, 외부 RAG에 연결된 추가 분야도
포함해 AI 튜터 검색과 AI 자료제작에서 같은 분류 체계를 사용합니다.

> 기존 과정·레슨·진도·이수 기능은 단순 읽음 표시의 실효성이 낮아 2026년 6월 제거했습니다.
> 잔존하던 `lesson_progress` 테이블도 2026년 8월 마이그레이션에서 삭제했습니다.
> 출동 마일리지와 체력단련 기능은 시범운영 범위 조정에 따라 2026년 8월 제거했습니다.

## 주요 화면

| 경로 | 기능 |
| --- | --- |
| `/login` | 비밀번호 또는 이메일 링크 로그인, 최초 로그인 비밀번호 변경 |
| `/home` | 공지, 핵심 업무, 최근 AI 대화와 구조 동향 |
| `/chat` | 교육자료 기반 AI 튜터 |
| `/chat/[conversationId]` | 저장된 대화 열람 |
| `/generate` | 훈련계획·교안·슬라이드 제작 및 결과 편집 |
| `/generate/saved` | 개인 생성물 저장, 재편집 및 부분 재생성 |
| `/generate/shared` | 사용자가 공유한 생성물 열람 및 복제 |
| `/docs` | 교육자료 목록 및 검색 |
| `/docs/[id]` | 원본 PDF 열람과 페이지 이동 |
| `/news` | 국내외 구조 동향과 신기술 사례 |
| `/notices` | 공지사항 |
| `/me` | 사용자 정보와 저장한 자료 요약 |
| `/admin/*` | 통계, 자료, 사용자, 공지 및 동향 관리 |

## 동작 흐름

```text
PDF/HWPX/HWP/DOCX/PPTX/이미지
        |
        v
rag7.py GUI -> 문서 변환·OCR -> 페이지 보존 청킹 -> 1024차원 임베딩
        |                                             |
        +--------> Supabase Storage + PostgreSQL/pgvector
                                                      |
사용자 질문 -> 하이브리드 검색(벡터+키워드) -> 재순위·컨텍스트 조립
                                                      |
                                                      v
                                            LLM 답변 + 출처·페이지
```

`rag7.py`는 신규 청크를 비활성 상태로 먼저 적재하고, 전체 배치 검증이 끝난 경우에만 새 버전을
활성화합니다. 새 버전 활성화와 이전 버전 교체는 하나의 DB 트랜잭션으로 처리하며, Storage
원본 등록은 단계별 검증과 실패 시 정리 절차를 거쳐 불완전한 데이터가 검색되지 않도록 합니다.

## 기술 스택

| 구분 | 구성 |
| --- | --- |
| 웹 | Next.js 15 App Router, React 18, TypeScript |
| UI | Tailwind CSS v4, shadcn/ui, Radix UI, Pretendard |
| AI | Vercel AI SDK v4, Claude·Gemini·OpenAI 호환 LLM 선택 |
| 데이터 | Supabase PostgreSQL, pgvector, Auth, 비공개 Storage, RLS |
| 검색 | 벡터·키워드 하이브리드 검색, 질의 확장, 선택적 재순위 |
| 임베딩 | 기본 `gemini-embedding-001` 1024차원, OpenAI·BGE-M3·Ollama 선택 |
| 인덱싱 | Python, Docling, EasyOCR, LibreOffice, `rag7.py` GUI |
| 문서 생성 | HWPX, DOCX, PptxGenJS |
| 검증 | Vitest, TypeScript, Next.js 프로덕션 빌드 |

LLM은 `LLM_PROVIDER=claude|gemini|openai-compat`, 임베딩은
`EMBEDDING_PROVIDER=google|openai|bge|ollama|auto`로 전환할 수 있습니다.

> 웹앱의 질의 임베딩과 인덱서의 문서 임베딩은 **제공자, 모델, 차원, 전처리 버전이 모두
> 같아야 합니다.** 임베딩 모델을 변경하면 기존 자료를 같은 설정으로 전체 재인덱싱해야 합니다.

## 빠른 시작

### 준비물

- Node.js 20.19 이상
- npm
- Supabase 프로젝트
- 사용할 LLM 및 임베딩 제공자의 API 키
- 자료 인덱싱 시 Python 3.11

### 웹앱 실행

```bash
npm install
cp .env.local.example .env.local
# .env.local 값을 환경에 맞게 설정
npm run dev
```

브라우저에서 `http://localhost:3000`으로 접속합니다. 환경변수 설명과 서버 전용 키 구분은
[`.env.local.example`](.env.local.example)을 기준으로 확인합니다.

실제 API와 Supabase 없이 UI 흐름만 시연할 때는 데모 모드를 사용할 수 있습니다.

```bash
NEXT_PUBLIC_DEMO_MODE=1 NEXT_PUBLIC_SUPABASE_URL=https://demo.supabase.co npm run dev
```

데모 모드는 미들웨어의 인증 검사를 통째로 통과시키므로, **실제 Supabase 백엔드가 연결된
환경에서는 플래그가 켜져 있어도 자동으로 무시**합니다(`lib/demo-flag.ts`). 그래서 위 명령처럼
Supabase 주소를 자리표시자로 덮어써야 데모가 켜집니다. 이 가드는 운영 배포에 데모 플래그가
실수로 들어가 인증이 열리는 사고를 막기 위한 것이므로 제거하지 마세요.

### 데이터베이스 준비

1. Supabase에서 `vector`, `pg_trgm` 확장을 사용할 수 있는 프로젝트를 준비합니다.
2. **새 프로젝트**라면 [`supabase/setup_new_project.sql`](supabase/setup_new_project.sql)
   전체를 SQL Editor에서 한 번에 실행합니다. 이 파일은 모든 마이그레이션에서 자동 생성되며
   (`npm run sql:setup`), 직접 수정하지 않습니다.
3. **기존 프로젝트**라면 `supabase/migrations/`에서 아직 적용하지 않은 파일만 파일명 순서로
   실행합니다. 규칙과 파일별 설명은
   [`supabase/migrations/README.md`](supabase/migrations/README.md)에 있습니다.
4. `20260828115838_allow_authenticated_document_downloads.sql`이 원본 자료용 비공개
   Storage 버킷 `documents`와 인증 사용자 읽기 정책을 함께 구성합니다. 수동으로 공개 버킷을
   만들지 않습니다.
5. 관리자 계정과 일반 사용자 계정을 등록합니다.
   일괄 등록은 `node scripts/import-users.mjs <명단.csv>`를 사용하며,
   `--random-password`를 붙이면 무작위 초기 비밀번호를 발급해 별도 CSV로 떨어뜨립니다
   (명단 유출만으로 로그인되지 않게 하려면 이쪽을 권장합니다).

세부 설치와 인증 설정은 [`SETUP.md`](SETUP.md), 실제 운영 전환 순서는
[`DEPLOYMENT.md`](DEPLOYMENT.md)를 참고합니다.

## RAG 자료 인덱싱

### GUI 인덱서

현재 PoC의 권장 경로는 루트의 `rag7.py`입니다.

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r indexing/requirements-rag7.txt
python rag7.py
```

- PDF 텍스트층을 페이지별로 검사하고 필요한 페이지만 한국어·영어 OCR을 적용합니다.
- HWPX/HWP는 LibreOffice로 임시 PDF를 만든 뒤 같은 파이프라인으로 처리합니다.
- DOCX, PPTX, TXT, Markdown, HTML 및 주요 이미지 형식도 선택할 수 있습니다.
- 업로드 전 추출 페이지, 문자 수, 청크 수와 청크 샘플을 검토할 수 있습니다.
- 업로드할 때 자료 유형을 `일반 교육자료`, `현장활동 지침·매뉴얼`, `표준작전절차(SOP)` 중에서
  지정합니다. 이 분류는 AI 자료제작에서 SOP 근거를 일반 교육 내용과 분리해 검증하는 데 쓰입니다.
- 원본 자동 등록을 켜면 자료실의 `documents` 행과 비공개 Storage 원본을 함께 연결합니다.
- 슬라이드 제작은 `발표형`과 `상세형`을 지원합니다. 원본 PDF의 페이지 근거가 확인된 장은
  다운로드 직전에 해당 페이지만 이미지로 변환해 PPTX에 넣고, 확인할 수 없으면 기본 도형으로
  안전하게 대체합니다.

HWPX/HWP 자동 변환에는 LibreOffice가 필요합니다. 인터넷이 차단된 내부망에서는 Docling과
EasyOCR 모델을 사전에 반입하고 `DOCLING_OCR_DOWNLOAD=0`으로 설정해야 합니다.

### 배치 인덱서

카테고리 폴더 단위의 일괄 적재에는 `indexing/embed_and_upload.py`를 사용할 수 있습니다.

```bash
cd indexing
pip install -r requirements.txt
python embed_and_upload.py
```

자세한 포맷·OCR·메타데이터 규칙은 [`indexing/README.md`](indexing/README.md)를 참고합니다.

## AI 자료제작 출력

| 유형 | 결과 |
| --- | --- |
| 훈련계획 | 전북소방 표준 양식 HWPX, DOCX, 텍스트 |
| 교육자료(교안) | HWPX, DOCX, 텍스트 |
| 슬라이드 | 내용별 레이아웃이 적용된 분야 색상 PPTX, 슬라이드별 근거가 담긴 발표자 노트 |

자료 유형·분야·주제·대상·시간만 선택하면 여러 원본에서 관련 근거를 선별합니다. 날짜·장소는
훈련계획에서만 선택적으로 입력하고, 인원·보유 장비·훈련 환경은 하나의 `현장 조건`에 적습니다.
생성 모델은 품질 우선으로 자동 선택하므로 사용자가 기술적인 모델명을 고를 필요가 없습니다.
`산악사고 대비 훈련`처럼 범위가 넓은 주제는 최근 본인 저장 자료와 겹침이 적은 세부 훈련
방향을 **훈련주제 도움받기**에서 교범 근거와 함께 먼저 제안하고, 사용자가 선택한 한 방향에 집중해 생성합니다. 구체적인
주제는 기존처럼 바로 생성합니다.
훈련계획은 고정 5개 항목,
교안은 실습형 7개 흐름으로 생성하며, 시간 합계·안전·평가·분량·중복·출처를 자동 점검해
필요할 때 최대 두 차례 보완합니다. 슬라이드는 전체 구성을 먼저 확정하고 2장씩 저장하므로 긴
정밀 모델 호출 하나가 끊겨 처음부터 다시 만드는 일을 줄입니다. 진행 화면에는 서버의 실제 단계,
진행률, 경과시간과 예상 완료 시각을 표시하고, Workflow 실행 연결이 확인된 뒤에는 탭을 닫아도 작업이 계속됩니다. 완료 주소를 다시
열면 본인 작업을 복원하며 품질 게이트를 통과한 결과만 완성본으로 표시합니다. 최초 예상은
훈련계획 약 7분, 교안 약 9분, 슬라이드 약 12~20분이지만 모델 혼잡과 보완 횟수에 따라 더 걸릴 수
있고 시간 초과 기준으로 사용하지 않습니다. 모든 결과에는 관련 SOP·표준절차의 적용 여부를 표시합니다.
관리자가 같은 분야의 자료를 `SOP` 또는 `현장활동 지침·매뉴얼`로 분류한 근거가 있으면 정확한
출처와 적용 내용을 넣고, 없거나 검색 상태를 확인할 수 없으면 시행 전 확인이 필요한 상태를
명시하며 절차를 추정하지 않습니다. SOP 필수 표식·출처 또는 고정 안내문이 편집 과정에서
사라진 결과는 저장·공유·복사·공식 파일 다운로드를 막고 다시 확인하도록 안내합니다.
공유 시에는 같은 계약을 Supabase DB 트리거가 다시 검사하므로, 로그인 사용자가 Data API를
직접 호출해 `shared=true`로 바꾸거나 공유된 본문을 미검증 내용으로 덮어쓰는 경로도 차단합니다.
보안 마이그레이션 적용 시 기존 훈련계획·교안·슬라이드 공유본은 본문을 보존한 채 비공개로
전환되며, 다시 생성·검증한 자료만 재공유할 수 있습니다.

생성 결과는 화면에서 직접 수정하거나 일부 섹션만 다시 생성할 수 있습니다. 슬라이드는 화면에서
실제 장표 비율로 확인하고 레이아웃·순서를 조정할 수 있습니다. 개인 저장본은 다시
편집할 수 있고, 사용자가 명시적으로 공유한 자료만 동료의 공유 자료 화면에 표시됩니다.

모든 생성물은 교육자료에 근거한 **AI 초안**이므로 실제 훈련에 사용하기 전에 담당 교관이 내용,
최신성, 현장 여건 및 안전 기준을 반드시 검토해야 합니다.

## 검증 명령

```bash
npm test           # Vitest 단위·통합 테스트
npm run build      # 프로덕션 빌드, 타입 및 Next.js 검사 포함
npm run typecheck  # TypeScript 타입 검사
npm run lint       # ESLint
npm run sql:setup  # 마이그레이션에서 setup_new_project.sql 재생성
```

RAG 평가셋과 실행 방법은 [`eval/README.md`](eval/README.md)를 참고합니다.

## 보안 주의사항

- `SUPABASE_SERVICE_ROLE_KEY`, LLM API 키, 임베딩 API 키는 서버 전용입니다.
- 서버 전용 키에 `NEXT_PUBLIC_` 접두사를 붙이거나 클라이언트 코드에서 참조하지 않습니다.
- `documents` Storage 버킷은 비공개로 유지하고 인증된 사용자에게만 서명 URL을 발급합니다.
- 토큰과 비밀번호를 Git 저장소, 로그, 문서 또는 채팅에 남기지 않습니다.
- 사용자 데이터 테이블에는 RLS를 적용하고 본인 데이터만 접근하도록 유지합니다.

인증·요청 처리는 `lib/auth.ts`를 단일 출처로 사용합니다.

| 위치 | 사용할 가드 | 하는 일 |
| --- | --- | --- |
| 페이지·레이아웃 | `requireUserAndProfile()` | 세션 확인 + 최초 로그인 비밀번호 변경 강제 |
| 일반 route handler | `requireApiUser()` | 세션 확인 + 비밀번호 미변경 계정 차단 |
| 관리자 route handler | `requireApiAdmin()` | 위 항목 + `admin` 역할 재확인 |

- route handler에서 `supabase.auth.getUser()`를 직접 호출하지 않습니다. 비밀번호를 바꾸지 않은
  계정이 API로 새어 들어옵니다(초기 비밀번호는 디지털식별번호라 명단을 아는 사람이 알 수 있습니다).
- 로그인 후 이동 경로 등 리다이렉트 파라미터는 반드시 `safeRedirectPath()`를 통과시킵니다.
  외부 URL과 `javascript:` 스킴을 차단합니다.
- LLM을 호출하는 엔드포인트에는 `rateLimit()`을 적용하고, 클라이언트가 보낸 대화 이력은
  `trimChatHistory()`로 개수와 길이를 제한합니다.
- 응답 보안 헤더(CSP, `X-Frame-Options`, HSTS 등)는 `next.config.mjs`에서 관리합니다.

## PoC 범위와 내부망 이전

- 외부 LLM과 Supabase를 사용하는 PoC 환경은 실제 운영 환경이 아닙니다.
- 내부망 이전 시 PostgreSQL/pgvector, 비공개 파일 저장소, OpenAI 호환 LLM 및
  BGE-M3/Ollama 임베딩 서비스를 내부 서버로 교체할 수 있습니다.
- 임베딩 제공자가 바뀌면 전체 자료 재인덱싱과 검색 품질 평가가 필요합니다.
- OCR 모델, Python 패키지, Node 패키지는 망 분리 전에 검증된 버전으로 반입해야 합니다.

상세한 이전 순서와 점검표는 [`내부망 이전 가이드.md`](내부망%20이전%20가이드.md)를 참고합니다.

## 문서

- [`PRD.md`](PRD.md): 초기 제품 요구사항과 RAG 기준
- [`SETUP.md`](SETUP.md): 설치, Supabase, 인증 및 배포 가이드
- [`DEPLOYMENT.md`](DEPLOYMENT.md): PoC에서 실제 플랫폼으로 전환하는 실행 계획
- [`내부망 이전 가이드.md`](내부망%20이전%20가이드.md): 내부 서버 이전 런북
- [`indexing/README.md`](indexing/README.md): 인덱싱 파이프라인 상세
- [`eval/README.md`](eval/README.md): RAG 평가셋과 실행 방법
- [`CLAUDE.md`](CLAUDE.md): 프로젝트 개발 규칙과 코딩 컨벤션
