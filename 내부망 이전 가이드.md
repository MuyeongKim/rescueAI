# 내부망(도청 서버) 이전 가이드

> **목적**: 현재 Vercel(프론트) + Supabase(백엔드) 구성을 나중에 **전북도청 내부망 서버**로
> 통째로 옮길 때의 단계별 런북. 프론트·DB·임베딩·LLM이 전부 도청 서버 안으로 들어간다.
> 작업이 추가될 때마다 이 문서를 갱신한다. (마지막 갱신: 2026-06-18)

설계 원칙: **하드코딩 금지 → 환경변수 + 마이그레이션 SQL만 교체하면 코드 변경 없이 이전.**

---

## 1. 현재 vs 목표

| 구성요소 | 현재 | 목표(내부망) |
|----------|------|--------------|
| 프론트엔드 | Vercel (Next.js 14) | 도청 서버 내 Node/Docker 호스팅 |
| DB·Auth·Storage | Supabase (클라우드) | Supabase self-hosted (또는 사내 Postgres+pgvector) |
| 임베딩 | 홈서버 Ollama bge-m3 (Tailscale Funnel 경유) | 내부망 Ollama bge-m3 (직접 접근) |
| LLM | Gemini API (외부) | 내부망 Qwen (openai-compat) |
| 인터넷 | 가능 | **차단 가정** → 외부 API(Gemini) 사용 불가 |

---

## 2. 이전 순서 (런북)

### ① DB 스키마 재현
내부망 Supabase/Postgres에 SQL을 순서대로 실행한다(전부 재실행 안전 가드 있음):
1. **벡터 테이블·검색함수**: `indexing/rag_rescue_schema.sql`
   (pgvector + `rag_rescue` 테이블 + `match_rag_rescue` RPC + 인덱스)
2. **Storage 버킷**(자료실 원본 PDF): `node scripts/create-storage-bucket.mjs`
   → 비공개 버킷 `documents` 생성(PDF·50MB). 열람은 서버가 만든 서명 URL로만.
3. **앱 스키마**: `supabase/setup_new_project.sql`
   (마이그레이션 0001~0007 합본 — profiles/conversations/messages/lesson_progress/notices/workout_logs
   + 트리거 + RLS + 직원필드·비번변경 플래그)
   - ⚠️ `handle_new_user` 트리거는 `set search_path=public` 필수(없으면 계정 생성이 "Database error"로 깨짐 — 0003에 반영됨).

### ② 자료(벡터) 적재
- 내부망에서 `rag7.py`(홈서버 도구, git 미추적) 실행 → `rag_rescue`에 청크 적재.
- **임베딩은 반드시 bge-m3 / 1024차원** (웹앱 EMBEDDING_DIM·인덱서 일치).
- 적재 시 분야(`edu_category`) 지정 — 챗봇 분야 필터·이수 판정 기준.

### ③ 계정 생성
- `node scripts/import-users.mjs 명단.csv`
- CSV 열: `email, full_name, division, rank, team, digital_id, role(선택)`
- 초기 비번 = digital_id, 첫 로그인 시 `/change-password` 강제(must_change_password).
- 관리자는 CSV에 `role=admin` 또는 등록 후 `profiles.role` 수동 변경.

### ④ 환경변수 교체 (`.env.local` / 호스팅 환경)
아래 키만 내부망 값으로 바꾸면 된다(코드 변경 없음):

| 키 | 현재 | 내부망 |
|----|------|--------|
| `NEXT_PUBLIC_SUPABASE_URL` | 클라우드 Supabase URL | 사내 Supabase URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 〃 | 사내 anon 키 |
| `SUPABASE_SERVICE_ROLE_KEY` | 〃 | 사내 service_role 키 |
| `NEXT_PUBLIC_SITE_URL` | Vercel 도메인 | 내부망 접속 주소 |
| `RAG_TABLE` | `rag_rescue` | `rag_rescue` (유지) |
| `EMBEDDING_PROVIDER` | `ollama` | `ollama` (유지) |
| `EMBEDDING_MODEL` | `bge-m3:latest` | 〃 |
| `EMBEDDING_API_URL` | 홈서버(Tailscale) | **내부망 Ollama 주소** |
| `LLM_PROVIDER` | `gemini` | **`openai-compat`** |
| `GEMINI_MODEL`/`GOOGLE_GENERATIVE_AI_API_KEY` | 사용 | **제거** |
| (신규) `LLM_API_URL` | — | 내부망 Qwen 주소 |
| (신규) `LLM_MODEL` | — | 예: `qwen3.5` |
| (신규) `LLM_API_KEY` | — | 필요 시(보통 `not-needed`) |
| `QUERY_EXPANSION`/`RERANK` | (기본 on) | 부하 크면 `0`으로 끌 수 있음 |

> 코드 근거: LLM은 `lib/llm.ts`(provider 분기), 임베딩은 `lib/embeddings.ts`,
> 테이블명은 `lib/rag2026.ts`의 `RAG_TABLE` 파생.

### ⑤ 네트워크
- 내부망에선 **Tailscale Funnel 불필요** — 프론트·Ollama·DB 모두 같은 망 안. 직접 주소 사용.
- 외부 인터넷 차단 시 Gemini 호출 불가 → ④의 LLM을 Qwen으로 전환해야 챗봇·자료제작 동작.

### ⑥ E2E 검증 체크리스트
- [ ] 로그인(비밀번호) 성공, 첫 로그인 계정은 `/change-password` 강제 동작
- [ ] 챗봇 질의 → `rag_rescue` 근거 답변 + 출처 칩 (예: "소방드론 SOP")
- [ ] 근거 없는 질문 → "확인되지 않습니다" (환각 가드)
- [ ] AI 자료제작(분야 컨텍스트) → docx/pptx 생성
- [ ] 자료실: 관리자 PDF 업로드 → `/docs`에서 열람(서명 URL)
- [ ] 관리자(tenmoo) admin 화면·통계 접근
- [ ] 공지/체력 기능 동작(마이그레이션 적용됐으므로) — ※ 학습/진도/이수는 제거됨(2026-06-18)
- [ ] `node eval/run.mjs` 정확도 측정

---

## 3. 미해결·확인 필요 (행정정보과 협의)
- GPU/RAM 사양 (Qwen·bge-m3 구동 가능 여부)
- 인터넷 차단 여부 (차단 시 Gemini→Qwen 필수)
- SMTP 가능 여부 (매직링크 메일 — 안 되면 비밀번호 로그인만, 현재도 비번 기본이라 무방)
- Supabase self-hosted vs 순수 Postgres (Auth/Storage 사용하므로 self-hosted 권장)

---

## 4. 변경 이력 (이전에 영향 주는 작업 누적)
- 2026-06-18: **자료실 원본 열람** — Storage 비공개 버킷 `documents`(서명 URL). 이전 시 `create-storage-bucket.mjs` 실행 필요.
- 2026-06-18: **레슨/진도/이수 기능 제거** — 학습/이수현황/`/api/progress` 라우트·`lib/learning`·학습 컴포넌트 삭제.
  `lesson_progress` 테이블은 미사용으로 잔존(이전 시 굳이 만들 필요 없음). documents는 자료실(원본 열람)용으로만.
- 2026-06-18: Supabase 새 프로젝트 교체, RAG 테이블 `rag_2026`→`rag_rescue` 개명·코드 일반화(RAG_TABLE 파생).
- 2026-06-18: 마이그레이션 0001~0007 전체 적용 방향(무마이그레이션 운영 종료). 0007=직원필드(rank/team/digital_id)+비번변경 플래그.
- 2026-06-18: `handle_new_user` 트리거 search_path 버그 수정(0003).
- 2026-06-18: 직원 일괄등록(초기 비번=digital_id) + 첫 로그인 비번 변경 강제 기능.
- 2026-06-15: RAG 쿼리 확장 + 목차 노이즈 필터, eval 러너 현재 경로로 교체.
