# DEPLOYMENT.md — 실전 플랫폼 전환 실행 계획

이 문서는 **데모 모드(목 데이터)로 도는 현재 상태를 실제 운영 플랫폼으로 전환**하는
실행 계획이다. 설치 절차 상세는 [`SETUP.md`](SETUP.md), 제품 명세는 [`PRD.md`](PRD.md),
규칙·컨벤션은 [`CLAUDE.md`](CLAUDE.md)를 본다.

> 한 줄 요약: **코드는 이미 웹앱(Next.js) + Supabase 형태로 완성되어 있다.
> 새로 만드는 것이 아니라, 데모 분기를 끄고 실제 백엔드·실제 자료를 연결한다.**

---

## 0. 현재 상태 진단 (2026-06 기준)

- PRD M0~M9 전 기능 구현 완료 + 학습 플랫폼 기능(과정/진도/퀴즈) 추가 구현됨.
- 단, `.env.local`이 `NEXT_PUBLIC_DEMO_MODE=1` + 가짜 URL뿐 → **전부 목 데이터로 동작 중**.
- `docs/`에 실제 교육자료 없음, `eval/`에 실전 평가셋 없음.
- 데모 분기는 [`lib/demo.ts`](lib/demo.ts)의 `if (DEMO)` 가드로 격리되어 있어
  **환경변수만 채우면 실제 흐름으로 전환**된다. (코드 수정 불필요)

→ 결론: 지금 할 일은 "코드 제작"이 아니라 **"백엔드 실연결 + 실제 자료 투입 + 검증"**.

---

## 1. 전환 실행 계획 (Phase 1~5)

### Phase 1 — 백엔드 실연결 (선행: 사용자 준비물 ①②)

- [ ] **P1-1.** Supabase 프로젝트 생성(서울 리전) — *사용자가 supabase.com에서 생성*
- [ ] **P1-2.** 마이그레이션 `supabase/migrations/0001~0005` 적용 (SQL Editor 또는 CLI)
      — `0005`는 LMS 확장(공지사항·체력단련 마일리지)
- [ ] **P1-3.** Storage `documents` 버킷 생성 + 매직링크 Auth 설정 ([`SETUP.md`](SETUP.md) §2~5)
- [ ] **P1-4.** `.env.local`에 실제 키 입력: Supabase URL/anon/service role,
      `ANTHROPIC_API_KEY`, `OPENAI_API_KEY` (목록·설명: `.env.local.example`)
- [ ] **P1-5.** `NEXT_PUBLIC_DEMO_MODE` 제거 → 데모 모드 해제
- [ ] **P1-6.** 로그인(매직링크) → 빈 상태 화면 정상 확인

### Phase 2 — 실제 교육자료 인덱싱 (선행: 사용자 준비물 ③) ★최중요

- [ ] **P2-1.** 실제 교육자료 PDF를 `docs/<분야>/` 에 투입 (산악/수난/화재/구급)
- [ ] **P2-2.** 인덱서 실행: `cd indexing && pip install -r requirements.txt && python embed_and_upload.py`
- [ ] **P2-3.** `documents`/`chunks` 적재 확인, 과정 자동 편성(`/courses`) 확인

> **자료 품질·양 = 챗봇 품질.** 자료를 넣지 않으면 챗봇은 "확인되지 않습니다"만 답한다.

### Phase 3 — 실기 검증 + 품질 기준선

- [ ] **P3-1.** 전체 흐름 실기 검증: 로그인 → 질문 → 출처 칩 → 자료 뷰어 점프 → 퀴즈 → 이수
- [ ] **P3-2.** 수용 기준 체크리스트(AC-1~11 / PL-1~5, [`SETUP.md`](SETUP.md)) 통과
- [ ] **P3-3.** 실제 자료 기반 **평가셋 50문항 작성** → [`eval/`](eval) 러너로 정확도 측정(60%↑)
      자료 갱신 시마다 재측정해 회귀를 막는다.
- [ ] **P3-4.** (측정 결과에 따라) 한국어 검색 보강 — `hybrid_search`의 `simple` tsvector는
      한국어 조사·활용에 약함. pg_trgm 보강 또는 형태소 토큰 정규화 검토.

### Phase 4 — 플랫폼 보강 (운영다운 모습 갖추기)

- [x] **P4-1.** **메뉴(IA) 확정 — 정식 LMS형으로 확정·구현됨.**
      일반: 홈 / 학습 / AI 튜터 / 자료실 / **체력단련(마일리지)** / 공지사항 / 마이페이지.
      관리자: 통계 / 자료 관리 / 사용자 관리 / 공지 작성.
      (모바일 하단 탭은 홈·학습·AI 튜터·체력단련·마이페이지 5개)
- [ ] **P4-2.** 관리자 자료 업로드 UI (현재는 Python 스크립트 대체 — `/admin/documents`에 안내 표시)
- [ ] **P4-3.** 계정 발급 — 명단 일괄 등록은 `scripts/import-users.mjs` 사용
      (엑셀 → CSV 저장: email, full_name, division, role). 관리자 승격은 `/admin/users`에서 가능.
- [ ] **P4-4.** 모니터링(에러 추적), 토큰 비용 한도·prompt caching
- [ ] **P4-5.** 개인정보·보존정책 — 대화/퀴즈 로그 보존·파기 기준, PIA 검토(공공기관 필수)

### Phase 5 — 운영 환경 확정 후 이전

기관과 협의해 아래 갈림길을 확정한 뒤 이전한다. **가장 먼저 확인할 것: 망분리 여부.**

| 결정 | 가벼움 ──────────────────→ 무거움 |
|------|-------------------------------------|
| **호스팅** | Vercel → 국내 클라우드(NHN·네이버·KT) → 행정망 내부 서버 |
| **DB** | Supabase 매니지드 → 국내 PostgreSQL + pgvector 셀프호스팅 |
| **LLM / 임베딩** | Claude+OpenAI API → BGE 임베딩 자체호스팅(*코드 옵션 있음*) → LLM까지 자체호스팅(*코드 수정 필요*) |

| 망 조건 | 작업 규모 | 핵심 작업 |
|---------|----------|----------|
| **외부 API 호출 가능** | 수일 규모 | 코드 거의 그대로. 환경변수·자료만 교체. |
| **망분리(외부 차단)** | 수주 규모 | LLM을 자체호스팅 모델로 교체하는 **코드 작업** + [`eval/`](eval) 품질 재검증. |

- 임베딩: BGE-M3 전환은 코드 옵션이 이미 있음(`EMBEDDING_PROVIDER=bge` + [`indexing/serve.py`](indexing/serve.py)).
- 임베딩 제공자를 바꾸면 **차원·모델을 동일하게 맞추고 자료를 재인덱싱**해야 하며,
  웹앱 쿼리 임베딩과 Python 인덱서는 **항상 같은 설정**을 써야 한다.
- 망분리 가능성이 높다면 `@ai-sdk/anthropic` 의존부를 프로바이더 추상화로 미리 분리해 두면
  전환 비용이 줄어든다.

---

## 2. 사용자(운영자) 준비물 체크리스트

코드 밖에서 마련해야 하는 것들. 이것이 준비되는 즉시 해당 Phase를 진행할 수 있다.

- [ ] ① **Supabase 프로젝트** — URL · anon key · service role key
- [ ] ② **API 키** — Anthropic(챗봇·퀴즈 생성), OpenAI(임베딩)
- [ ] ③ **실제 교육자료 PDF** — 분야별(산악/수난/화재/구급)로 분류된 원본
- [ ] ④ (Phase 5) 기관의 호스팅·망분리 정책 답변

---

## 3. 권장 진행 순서 요약

1. **지금:** 준비물 ①②③ 확보 → Phase 1~2 (실연결 + 자료 인덱싱)
2. **그 다음:** Phase 3 (검증 + 평가 기준선) — "진짜 자료로 도는 플랫폼" 완성
3. **병행:** 기관과 운영 환경 협의 (준비물 ④)
4. **운영 직전:** Phase 4 보강 → Phase 5 이전

---

## 참고 문서
- [`SETUP.md`](SETUP.md) — 설치·실행·배포 상세 절차, 수용 기준 체크리스트
- [`PRD.md`](PRD.md) — 제품 명세
- [`CLAUDE.md`](CLAUDE.md) — 기술 스택·보안 규칙·컨벤션
- `indexing/README.md` — 자료 인덱싱 파이프라인
