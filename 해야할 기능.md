# 해야 할 기능 (TODO)

전북소방 구조 교육훈련 플랫폼 — 미구현·대기 기능 정리.
작성 2026-06-15. 서식·홈서버 환경이 정해지면 진행.

---

## 1. 파일 업로드 (2종 — 목적이 완전히 다름)

업로드는 **두 가지 별개 기능**이다. 하나만 한다고 다른 게 되지 않는다.

### ① 원본 파일 저장 (자료실)
- **목적**: 대원이 원문을 열람·다운로드 (SOP·매뉴얼 PDF 원본)
- **저장 위치**: Supabase **Storage** (파일 그대로)
- **처리**: 파싱·임베딩 불필요 — 그냥 업로드
- **환경**: 웹앱(Next.js)만으로 **바로 구현 가능**
- **연결**: 자료실(`/docs`) 목록·PDF 뷰어, 챗봇 출처 칩 → 원본 보기
- **현재 상태**: 미구현 (`/docs`·자료 관리 페이지는 목/조회 전용)

### ② 벡터DB 적재 (RAG)
- **목적**: 챗봇·AI 자료제작이 검색·근거로 활용
- **저장 위치**: `rag_2026` 테이블 (청크 텍스트 + 1024차원 임베딩)
- **처리**: docling 파싱 → 헤더 청킹 → bge-m3 임베딩 → 적재 + 분야(edu_category) 지정
- **환경**: **파이썬·홈서버 필요** (docling·Ollama). 웹앱에서 직접 불가.
- **현재 상태**: 홈서버 `rag7.py`(729줄 GUI 도구)가 담당 중. 3,885청크 적재됨.

### 구현 방향
- **단기(권장)**: ①만 웹앱에 구현 + ②는 rag7.py 계속 사용.
  자료 관리 페이지 안내문을 rag7.py 기준으로 수정.
- **중기**: 관리자 업로드 1회로 ①+② 동시 처리 →
  홈서버에 rag7.py 로직을 **API 서버**로 띄우고(파일받기→파싱→임베딩→적재),
  웹앱이 ① Storage 저장 + ② 그 API 호출.
- **출처 연결 과제**: rag_2026의 `source`(파일명)와 ① Storage 원본을 매칭해야
  챗봇 출처에서 원문으로 점프 가능 (현재 `document_id=0`이라 원본 링크 숨김).

---

## 2. 생성 서식(템플릿) 적용 — **서식 제공 필요**

AI 자료제작 산출물이 **전북소방 표준 양식**으로 일관되게 나오게 한다.
서식은 사용자가 제공 예정. 받는 즉시 코드 템플릿에 반영.

### 받을 형식 (둘 중 편한 것)
- 실제 한글/PPT 파일 샘플, 또는
- 섹션별/슬라이드별 구성 목록 (표지에 뭐, 1장에 뭐 …)

### 대상
- **훈련계획 서식폼 재구성**: 입력 폼 항목(분야·대상·시간·주제·일자·모델)을
  훈련계획 서식에 맞춰 재구성. 추가 항목 예: 훈련 장소·인원·강사·차시 등.
  → 서식이 정해져야 폼 항목 확정 (2.훈련계획 서식과 짝).
- **슬라이드(PPTX) 서식**: `lib/pptx.ts` 템플릿을 제공 양식대로 맞춤
  (표지·헤더·색·발표자 노트 배치).
- **훈련계획/교안 한글(hwpx) 서식**: `lib/hwpx.ts` 템플릿을 제공 양식대로 맞춤.
- **현재 상태**: 임시 표준 양식으로 생성 동작 중. 서식 확정 시 교체.

---

## 3. 기타 대기 항목

- **챗봇 LLM 선택**: AI 자료제작 폼엔 모델 선택(제미나이 플래시/프로) 있음.
  챗봇(AI 튜터)에도 넣을지 결정 필요 (현재 챗봇은 서버 기본 모델 고정).
- **답변 품질 평가셋 — 50문항 작성**: 러너는 현재 운영 경로(쿼리 확장→Ollama
  bge-m3→`match_rag_2026`→재순위→Gemini)로 교체 완료(`eval/run.mjs` → vitest 통합 러너).
  예시 6문항만 있고 실제 `eval/questions.jsonl` 미작성. **현재 코퍼스(SOP·화학·드론
  매뉴얼·장비분류·복무)에 맞춰** 50문항을 작성해야 정확도가 의미 있어진다.
  (예시셋의 공기호흡기·유압전개기 문항은 전용 절차가 코퍼스에 없어 가드레일이 정상 거부 → 현재 4/6.)
  작성 후 `node eval/run.mjs eval/questions.jsonl` 로 프롬프트·검색 파라미터 A/B 측정.
- **학습/진도/공지/체력 기능**: 마이그레이션(0001~0006) 미적용 상태.
  실제 사용하려면 SQL Editor 실행 필요 (챗봇·자료제작은 마이그레이션 없이 동작).

---

## 4. 내부망 이관 시 (별도 메모)

- VM + Docker: Supabase self-hosted + Ollama(bge-m3 + Qwen) + 웹앱
- LLM: Gemini → 내부망 Qwen (`LLM_PROVIDER=openai-compat`, 주소만 교체)
- 로그인: 비밀번호 방식 적용됨 (SMTP 불필요)
- 행정정보과 협의 확인: GPU/RAM 사양, 인터넷 차단 여부
