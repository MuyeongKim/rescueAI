# 해야 할 기능 (TODO)

전북소방 구조 교육훈련 플랫폼 — 미구현·대기 기능 정리.
작성 2026-06-15 · 갱신 2026-06-21. 서식·홈서버 환경이 정해지면 진행.
(완료된 항목은 ~~취소선~~ + ✅ 로 표시한다.)

---

## 1. 벡터DB 적재 (RAG) · 출처 연결

> 원본 파일 저장(자료실 Storage 비공개 버킷 `documents`)은 **구현 완료**. 남은 것은 ② 벡터DB 적재와의 통합·출처 연결.

### ② 벡터DB 적재 (RAG)
- **목적**: 챗봇·AI 자료제작이 검색·근거로 활용
- **저장 위치**: `rag_rescue` 테이블 (청크 텍스트 + 1024차원 임베딩)
- **처리**: docling 파싱 → 헤더 청킹 → bge-m3 임베딩 → 적재 + 분야(edu_category) 지정
- **환경**: **파이썬·홈서버 필요** (docling·Ollama). 웹앱에서 직접 불가.
- **현재 상태**: 홈서버 `rag7.py`가 담당. **(2026-06-18 새 Supabase 프로젝트로 이전 — `rag_rescue` 0행,
  다른 PC에서 재적재 예정.** 스키마는 `indexing/rag_rescue_schema.sql`로 생성됨.)

### 구현 방향 (남은 것)
- **현재**: 원본 저장은 웹앱(Storage), 벡터 적재는 홈서버 rag7.py 분리 운용.
- **중기**: 관리자 업로드 1회로 원본 저장 + 벡터 적재 동시 처리 →
  홈서버에 rag7.py 로직을 **API 서버**로 띄우고(파일받기→파싱→임베딩→적재),
  웹앱이 Storage 저장 + 그 API 호출.
- **출처 칩 → 원본 PDF 점프 (보류 — 실제 파일 업로드 후)**: 현재 AI 튜터 출처 칩은 링크 없음
  (`rag_rescue` 청크가 `document_id=0`이라 `SourceBadge`가 링크 숨김). rag_rescue의 `source`(파일명)와
  Storage 원본(`documents`)을 **파일명으로 매칭**하면 출처 칩 클릭 → 그 PDF 열람(`/docs/<id>`) 가능.
  - **문서 단위 점프**: 파일명 매칭으로 바로 구현 가능. **실제 자료를 올린 뒤** 진행하기로 함(2026-06-18).
  - **페이지 단위 점프**: rag_rescue 메타데이터에 페이지 번호가 없어 불가 → 인덱싱(rag7.py)에서 page 메타 추가 필요.

---

## 2. 생성 서식(템플릿) 적용 — **서식 제공 필요**

AI 자료제작 산출물이 **전북소방 표준 양식**으로 일관되게 나오게 한다.
서식은 사용자가 제공 예정. 받는 즉시 코드 템플릿에 반영.

### 받을 형식 (둘 중 편한 것)
- 실제 한글/PPT 파일 샘플, 또는
- 섹션별/슬라이드별 구성 목록 (표지에 뭐, 1장에 뭐 …)

### 대상
- **생성 스키마·구성 재정의 (★ 서식 확정 시 필수 수정)**: `lib/generate.ts`의 결과 스키마
  (`generatedDocSchema`·`generatedSlidesSchema`)와 유형별 구성(`TYPE_GUIDE`·`buildGeneratePrompt`)을
  제공 서식에 맞춰 수정한다. 서식이 정한 **섹션·필드 구조**(예: 훈련계획의 장소·인원·강사·차시,
  교안의 고정 단원 구조)대로 AI가 채우도록 스키마 항목·프롬프트 구성을 재정의.
  부분 재생성 스키마(`regeneratedSectionSchema`·`regeneratedSlideSchema`)도 동반 수정.
- **훈련계획 서식폼 재구성**: 입력 폼 항목(분야·대상·시간·주제·일자·모델)을
  훈련계획 서식에 맞춰 재구성. 추가 항목 예: 훈련 장소·인원·강사·차시 등.
  → 서식이 정해져야 폼 항목 확정 (위 스키마 재정의와 짝).
- **슬라이드(PPTX) 서식**: `lib/pptx.ts` 템플릿을 제공 양식대로 맞춤
  (표지·헤더·색·발표자 노트 배치).
- **훈련계획/교안 한글(hwpx) 서식**: `lib/hwpx.ts` 템플릿을 제공 양식대로 맞춤.
- **현재 상태**: 임시 범용 스키마(문서=제목+섹션 3~8개, 슬라이드=제목+6~20장)로 동작 중.
  **서식은 사용자가 추후 제공 예정 — 받는 즉시 스키마·프롬프트·파일 템플릿 교체.**

---

## 3. 기타 대기 항목

- **답변 품질 평가셋 — 코퍼스 맞춤 보정**: `eval/questions.jsonl` 50문항 초안 작성됨(분야별 + 환각차단/의료거부).
  러너는 운영 경로(쿼리 확장→bge-m3→`match_rag_rescue`→재순위→Gemini)로 동작. **실제 자료 적재 후**
  코퍼스에 맞춰 문항·키워드 보정하고 `node eval/run.mjs eval/questions.jsonl` 로 측정.
- **직원 명단 적재 (대기)**: 일괄등록 도구(`import-users.mjs`, 계급·팀·디지털식별번호, 초기 비번=digital_id)와
  첫 로그인 비번 변경 강제(`/change-password`)는 구현됨. **0007 마이그레이션·실제 명단 적재만 대기.**
- **구조 동향 — 후속(선택)**: DB 큐레이션·자동수집(Google News RSS → AI 요약 → 매일 Cron)은 구현됨.
  남은 것: 네이버 뉴스 검색 API(국내 정확도↑, 키 받으면 `/api/news/refresh`에 소스 추가);
  타 매체 동일 사건 제목 유사도 dedup(현재 url만 중복제거).
- **회원가입 페이지**: 보류. 초기 명단은 일괄등록 채택. 신규 입사자용 가입(가입코드/도메인 게이트)은 추후 필요 시.

---

## 4. 내부망(도청 서버) 이관 시

> **상세 단계 런북은 `내부망 이전 가이드.md` 단일 출처.** (작업 시마다 그 문서 "변경 이력" 갱신)
> 프론트·DB 모두 도청 서버 안으로 들어감.

- VM + Docker: Supabase self-hosted + Ollama(bge-m3 + Qwen) + 웹앱
- LLM: Gemini → 내부망 Qwen (`LLM_PROVIDER=openai-compat`, 주소만 교체)
- ✅ 로그인: 비밀번호 방식 적용됨 (SMTP 불필요)
- 행정정보과 협의 확인: GPU/RAM 사양, 인터넷 차단 여부
